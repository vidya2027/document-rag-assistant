# Documents

Place your knowledge-base files in this folder.

Supported formats:

- `.pdf`
- `.txt`

The loader searches this folder recursively, so subfolders are allowed.

No hardcoded fallback document is used. If this folder contains no readable PDF or TXT files, the application stops with a clear error.
